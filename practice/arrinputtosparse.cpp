//array input to sparse matrix 

#include <stdio.h>

int main(){
    int n, m;
    printf("Input the number of rows in the matrix : ");
    scanf("%d", &n);
    printf("Input the number of columns in the matrix : ");
    scanf("%d", &m);

    int array[n][m];
    
    return 0;
}