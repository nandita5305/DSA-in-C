#include <stdio.h>

int main(){
    void *ptr = NULL;
    printf("The pointer ptr is a NULL pointer as it does not contain any stored adress\n");
    printf("Size of pointer PTR is %d", sizeof(NULL));
    return 0;
}